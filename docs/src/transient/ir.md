# Transient IR — 电阻瞬态响应

**测试文件**: `tests/test_trans_ir.rs`
**测试函数**: `trans_ir`

## 电路

```
    I1 (3A)
    ┌──→──┐
    │     │
    0     1
    │     │
    │    R1(10Ω)
    │     │
    └─────┘
    0
```

**程序化网表**:
```spice
I1 0 1 3.0
R1 1 0 10
```

## 解析

纯电阻电路的瞬态分析——最简单的瞬态测试。

- 电阻没有状态，所有时间步的电压都相同
- 每个时间步：\\(V_1 = 30V\\)
- 验证瞬态循环框架：时间步进、收敛判断、波形输出

## 验证

在每个时间步检查 \\(V_1 = 30V\\)。
